import { DEFAULT_V1_TIMELINE_ZOOM } from '@/editor-v1/normalization-types';

export const DEFAULT_PIXELS_PER_SECOND = DEFAULT_V1_TIMELINE_ZOOM;
export const MIN_PIXELS_PER_SECOND = 10;
export const MAX_PIXELS_PER_SECOND = 500;
export const ZOOM_STEP = 1.25;

export const PIXELS_PER_SECOND = DEFAULT_PIXELS_PER_SECOND;

export const TIMELINE_H_PADDING = 12;
